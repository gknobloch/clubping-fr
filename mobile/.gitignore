node_modules/
.expo/
dist/
web-build/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
google-play-service-account.json
# The FCM v1 service account (#495). Not the same file as
# google-services.json below: this one is a private key that can send a push
# to every installed copy of the app, and it is only ever uploaded to EAS.
google-fcm-service-account.json
*.orig.*
android/
ios/
.env

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
# fastlane (#492). The store notes are generated from CHANGELOG.md, which is the
# reviewed source; the Play file is named for a versionCode EAS only assigns at
# build time, so it cannot exist when the bump PR is reviewed anyway.
fastlane/metadata/
fastlane/build-context.json
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/
fastlane/test_output/
vendor/
.bundle/
